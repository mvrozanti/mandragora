data=//\t\t\tfor (Map.Entry<String, Bounds> entry : cod_modelo_etapa_bounds.entrySet()) { //\t\t\t\tString cod_modelo_etapa_bounds = entry.getKey();\n//\t\t\t\tBounds bounds = entry.getValue();\n//\t\t\t\tString cmd_sql = "INSERT INTO tab_rel_etapa_area VALUES (?,?)";\n//\t\t\t\tPreparedStatement ps;\n//\t\t\t\ttry {\n//\t\t\t\t\tps = DataBaseUtils.getConnection().prepareStatement(cmd_sql);\n//\t\t\t\t\tps.setString(1, cod_modelo_etapa_bounds);\n//\t\t\t\t\tps.setString(2, idc_area);\n//\t\t\t\t} catch (SQLException ex) {\n//\t\t\t\t\tLogger.getLogger(BPMUtils.class.getName()).log(Level.SEVERE, null, ex);\n//\t\t\t\t}\n//\t\t\t}\n
type=1
