#!/bin/bash
jpg_ext="jpe?g" 
if [[ `echo "$@" | rev | cut -d'.' -f1 | rev` =~ $jpg_ext ]]; then
    convert "$@" png:- | xclip -selection clipboard -t image/png
else
    xclip -selection clipboard -t image/png "$@"
fi
