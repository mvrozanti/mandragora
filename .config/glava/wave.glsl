/* min (vertical) line thickness */
#define MIN_THICKNESS 1

/* max (vertical) line thickness */
#define MAX_THICKNESS 3

/* base color to use, distance from center will multiply the RGB components */
#define BASE_COLOR vec4(1.0, 1.0, 0.0, 1)

/* amplitude */
#define AMPLIFY 2000

/* outline color */
#define OUTLINE vec4(0.0, 0.0, 0.0, 0.1)
