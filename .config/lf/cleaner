#!/bin/sh
# No-op: images are rendered via kitty's Unicode-placeholder mode, so replacing
# the placeholder cells with text (which lf does on its next preview) removes
# the image automatically. No graphics-protocol clear needed.
exit 0
