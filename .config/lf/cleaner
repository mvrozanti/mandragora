#!/bin/sh
# Tell ueberzugpp to remove the current preview image, if any.
printf '[%s] cleaner args=%s\n' "$(date +%H:%M:%S.%N)" "$*" >> /tmp/lf-cleaner.log
[ -n "${UEBERZUG_SOCKET:-}" ] || exit 0
ueberzugpp cmd \
  -s "$UEBERZUG_SOCKET" \
  -a remove \
  -i lf-preview 2>>/tmp/lf-cleaner.log
exit 0
