# Replaced during migration with the contents of /etc/openvpn/server.conf
# pulled from oracle (without `ca`, `cert`, `key`, `tls-crypt`, `crl-verify`
# directives — those are appended by modules/vps/openvpn.nix from sops paths
# and from environment.etc copies).
#
# Until the real config is dropped in here, mandragora-vps WILL NOT BUILD
# because builtins.readFile returns the literal contents of this placeholder.
