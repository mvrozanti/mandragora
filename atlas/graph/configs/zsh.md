---
type: config
tags: [config, shell]
path: .config/zsh/
---

# .config/zsh

`zshrc.zsh` + `p10k.zsh`. Read into the Nix store at build time, not loaded from disk at runtime.

Consumed by: [[../modules/user/zsh]], [[../modules/user/home]]
Touches: [[../concepts/language-purity]]
