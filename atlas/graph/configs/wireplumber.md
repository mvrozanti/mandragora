---
type: config
tags: [config, audio]
path: .config/wireplumber/
---

# .config/wireplumber

Pipewire session policy. The in-tree `hdmi-default.conf` sets HDMI as the default sink.

Consumed by: [[../modules/desktop/hyprland]] (read in via `builtins.readFile`)
Touches: [[../concepts/audio-pipeline]]
