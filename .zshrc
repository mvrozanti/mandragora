# =====================
# .zshrc — Delegates to ~/.config/zsh/init.zsh
# =====================
# To rollback: cp ~/.zshrc.backup ~/.zshrc && source ~/.zshrc
# =====================

export ZSH_CONFIG="$HOME/.config/zsh"
source $ZSH_CONFIG/init.zsh
