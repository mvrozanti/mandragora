# cv-es.mvr.ac — Edgard's CV workshop
# Real .env lives on the VPS under /home/opc/cv-es/.env; this file documents the schema.

# bcrypt hash for the single basic_auth user "edgard" (Apache htpasswd format)
CV_ES_BCRYPT=$2y$12$replace_with_real_hash

# Tailnet IP + port of the desktop edgard-web.service. Default matches the desktop.
CV_ES_UPSTREAM=100.115.80.79:7691
